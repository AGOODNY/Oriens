# 阶段 1 验收记录

更新日期：2026-08-10

## 自动化验收

- [x] 阶段 0 协议、状态与日志轮转测试保持通过。
- [x] 固定资料覆盖道具 ID `1`、`3`、`4`、`12`、`350`，每条资料包含 HTTPS 来源。
- [x] 模拟模型能生成短建议、理由、置信度、来源、状态序号与零费用统计。
- [x] 非法 JSON、额外/缺失字段、伪造来源和错误状态序号均不会展示。
- [x] 模型请求支持 8 秒超时、最多 1 次重试、取消标志和换房过期检查。
- [x] 网络模型失败后回退到本地模拟建议，不使 UI 崩溃。
- [x] 没有密钥时程序可启动为离线模拟模式。
- [x] 正常自动化测试只使用模拟适配器，不调用付费 API。
- [x] PySide6 离屏测试已验证日志事件能够更新道具、短建议、来源与零费用显示，并可无残留关闭。

执行命令：

```powershell
$env:PYTHONPATH = "$PWD\sidecar\src"
D:\python\python.exe -m unittest discover -s sidecar\tests -v
D:\python\python.exe -m oriens.cli advice-demo 350
D:\python\python.exe -m oriens.cli replay data\recordings\phase0-30min-20260809-102716.jsonl
```

## 人工与显式授权验收

- [x] 实际悬浮窗与游戏并排运行，核对当前房间、生命、硬币、钥匙、炸弹与识别道具。
- [x] 过期回复不会覆盖当前 UI：使用真实 `OverlayWindow` 注入换房事件与延迟旧回复的自动化测试通过。
- [x] 在用户确认后完成一次真实百炼烟雾测试：`qwen3.7-flash`，耗时约 11.1 秒，输入 185 Token、输出 82 Token，估算费用人民币 0.0001026 元；响应通过 JSON、来源、状态序号和最终展示 schema 校验，未回退到模拟模型。

### 第一次游戏画面对照

2026-08-10 的实机核对确认：连接状态、红心、魂心、硬币、钥匙、炸弹均与画面一致；在道具房执行 `spawn 5.100.350` 后能正常显示短建议。核对同时发现 UI 房间名称映射错误：`ROOM_CURSE=10` 被误标为恶魔/天使房，`ROOM_SECRET=7` 与 `ROOM_SUPERSECRET=8` 缺少中文名称。代码随后按游戏随附 `resources/scripts/enums.lua` 修正并补齐 `RoomType 0–30`。

重新启动悬浮窗后的第二次实机核对确认：刺房显示“诅咒房（刺房）”，普通隐藏房显示“隐藏房”，超级隐藏房显示“超级隐藏房”。阶段 1 的游戏画面与 Python 重建状态人工对照通过。

人工画面对照时请记录：游戏版本、道具名称/ID、房间类型、角色资源、Oriens 状态序号，以及是否一致。只要任一字段不一致，就不要勾选通过，应保留对应日志片段用于修正。
